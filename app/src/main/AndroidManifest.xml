<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.pedrojtmartins.racingcalendar">

    <!--
        Vibrate permission is required only in old versions
        due to changes in notification vibration policies
    -->
    <uses-permission
        android:name="android.permission.VIBRATE"
        android:maxSdkVersion="18"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="android.max_aspect"
            android:value="2.1"/>

        <activity
            android:name=".views.activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activities.FavoritesActivity"
            android:parentActivityName=".views.activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".views.activities.MainActivity"/>
        </activity>
        <activity
            android:name=".views.activities.SettingsActivity"
            android:parentActivityName=".views.activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".views.activities.MainActivity"/>
        </activity>
        <activity
            android:name=".views.activities.NotificationsActivity"
            android:parentActivityName=".views.activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".views.activities.MainActivity"/>
        </activity>
        <activity
            android:name=".views.activities.AboutActivity"
            android:parentActivityName=".views.activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".views.activities.MainActivity"/>
        </activity>
        <activity
            android:name=".views.activities.ResultsActivity"
            android:parentActivityName=".views.activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".views.activities.MainActivity"/>
        </activity>

        <!-- Notifications receiver and service -->
        <!-- Updated for api 26 -->
        <receiver android:name=".alarms.RCAlarmBroadcastReceiver"/>
        <service
            android:name=".notifications.RCNotificationService"
            android:permission="android.permission.BIND_JOB_SERVICE"/>

        <!-- This needs to happen to reset the alarms. When -->
        <!-- the device is turned off all alarms are cleared -->
        <!-- Updated for api 26 -->
        <receiver
            android:name=".alarms.RCAlarmResetBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
        <service
            android:name=".alarms.RCAlarmResetService"
            android:permission="android.permission.BIND_JOB_SERVICE"/>

        <!--Weekly notification-->
        <receiver android:name=".alarms.RCWeeklyAlarmBroadcastReceiver" />
        <service
            android:name=".notifications.RCWeeklyNotificationService"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <!--Widget-->
        <!--<receiver android:name=".widgetProviders.RemainingTimeWidget">-->
        <!--<intent-filter>-->
        <!--<action android:name="android.appwidget.action.APPWIDGET_UPDATE" />-->
        <!--</intent-filter>-->
        <!--<meta-data-->
        <!--android:name="android.appwidget.provider"-->
        <!--android:resource="@xml/remaining_time_widget_provider" />-->
        <!--</receiver>-->
        <!--<activity android:name=".views.widgetsConfig.RemainingTimeWidgetConfigActivity">-->
        <!--<intent-filter>-->
        <!--<action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />-->
        <!--</intent-filter>-->
        <!--</activity>-->
    </application>
</manifest>